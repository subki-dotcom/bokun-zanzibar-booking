const TourMetaRow = ({ icon, label }) => (
  <div className="tour-meta-row">
    <span className="tour-meta-icon">{icon}</span>
    <span className="tour-meta-label">{label}</span>
  </div>
);

export default TourMetaRow;
