import MeetingPickupSection from "./MeetingPickupSection";

const MeetingPickupCards = ({ meetingInfo = "", pickupInfo = "" }) => (
  <MeetingPickupSection meetingInfo={meetingInfo} pickupInfo={pickupInfo} />
);

export default MeetingPickupCards;

