import{useCallback,useEffect,useMemo,useState}from'react';import{Button,Form,Spinner}from'react-bootstrap';import{BsArrowClockwise,BsDownload,BsInfoCircleFill,BsLayers,BsPieChart,BsWallet2}from'react-icons/bs';import{exportFixedAssets,fetchFixedAssets}from'../../api/adminApi';import'./fixedAssets.css';
const money=(v,c)=>v==null||!c?'Unavailable':new Intl.NumberFormat('en-US',{style:'currency',currency:c,maximumFractionDigits:2}).format(Number(v));const date=v=>v?new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(v)):'—';const label=v=>String(v||'').toLowerCase().replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
const Kpi=({title,value,note,icon:Icon,tone=''})=><article className={`fa-kpi ${tone}`}><i><Icon/></i><div><small>{title}</small><strong>{value}</strong><span>{note}</span></div></article>;
const CategoryChart=({rows=[],currency})=>{const total=rows.reduce((s,r)=>s+Number(r.cost||0),0),colors=['#18aaa1','#3188eb','#68bd7d','#f2aa37','#d34d76','#93a4b8'];let x=0;const bg=rows.map((r,i)=>{const a=x;x+=total?Number(r.cost)/total*360:0;return`${colors[i%colors.length]} ${a}deg ${x}deg`}).join(',');return <div className="fa-category">{total?<div className="fa-donut" style={{background:`conic-gradient(${bg})`}}><span><b>{money(total,currency)}</b><small>Total cost</small></span></div>:<p>No category values available.</p>}<div>{rows.map((r,i)=><p key={r.category}><i style={{background:colors[i%colors.length]}}/><span>{r.category}<small>{r.count} asset{r.count===1?'':'s'}</small></span><b>{total?(Number(r.cost)/total*100).toFixed(1):0}%</b></p>)}</div></div>};
export default function AdminFixedAssetsPage(){const[state,setState]=useState({loading:true,data:null,error:''});const[filters,setFilters]=useState({search:'',category:'all',status:'all'});const load=useCallback(async()=>{setState(s=>({...s,loading:true,error:''}));try{setState({loading:false,data:await fetchFixedAssets(),error:''})}catch{setState({loading:false,data:null,error:'Unable to load Fixed Assets.'})}},[]);useEffect(()=>{load()},[load]);const d=state.data||{},s=d.summary||{},c=s.currency||'';const items=useMemo(()=>{const q=filters.search.toLowerCase();return(d.items||[]).filter(r=>(filters.category==='all'||r.category===filters.category)&&(filters.status==='all'||r.status===filters.status)&&(!q||[r.assetReference,r.name,r.category,r.location].some(v=>String(v||'').toLowerCase().includes(q))))},[d.items,filters]);const download=async()=>{const blob=await exportFixedAssets({format:'csv'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='fixed-assets.csv';a.click();URL.revokeObjectURL(url)};return <main className="fa-page"><header className="fa-header"><div><nav>Business Accounting / Assets / <strong>Fixed Assets</strong></nav><h1>Fixed Assets</h1><p>Manage fixed assets, depreciation, useful life, book value, and asset history.</p></div><div><Button disabled title="Asset acquisition posting workflow is not yet enabled">+ Add Asset</Button><Button variant="outline-secondary" disabled title="Validated import workflow is not available">Import</Button><Button variant="outline-secondary" onClick={download} disabled={!d.items?.length}><BsDownload/> Export</Button><Button variant="outline-secondary" onClick={load}><BsArrowClockwise/> Refresh</Button></div></header>{state.error?<div className="fa-error" role="alert">{state.error}<Button size="sm" onClick={load}>Retry</Button></div>:state.loading?<div className="fa-loading"><Spinner/><span>Loading asset register…</span></div>:<>{s.mixedCurrencies?<div className="fa-warning">Asset totals contain multiple currencies and cannot be consolidated without verified historical FX.</div>:null}{d.warnings?.map(w=><div className="fa-warning" key={w}>{w}</div>)}<section className="fa-kpis"><Kpi title="Total Fixed Assets" value={money(s.totalCost,c)} note={`${d.count||0} assets`} icon={BsLayers}/><Kpi title="Scheduled Depreciation" value={money(s.scheduledAccumulatedDepreciation,c)} note="Estimate; verify posted GL" icon={BsPieChart} tone="red"/><Kpi title="Estimated Net Book Value" value={money(s.netBookValue,c)} note="Cost less scheduled depreciation" icon={BsWallet2} tone="blue"/><Kpi title="Monthly Depreciation" value={money(s.monthlyDepreciation,c)} note="Straight-line schedule" icon={BsPieChart} tone="amber"/></section><section className="fa-analytics"><article><h2>Asset Value Overview</h2>{d.items?.length?<div className="fa-value-bars">{d.items.slice(0,12).map(r=><div key={r.assetReference}><span title={`${r.name}: ${money(r.purchaseCost,r.currency)}`} style={{height:`${Math.max(4,Number(r.purchaseCost)/Math.max(...d.items.map(x=>Number(x.purchaseCost||0)),1)*150)}px`}}/><small>{r.assetReference}</small></div>)}</div>:<p className="fa-empty">Historical snapshots are unavailable. Value trends will appear when asset history is recorded.</p>}</article><article><h2>Assets by Category</h2><CategoryChart rows={d.categories} currency={c}/></article></section><section className="fa-register"><header><h2>Fixed Assets</h2><div><Form.Control placeholder="Search assets…" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})}/><Form.Select aria-label="Category" value={filters.category} onChange={e=>setFilters({...filters,category:e.target.value})}><option value="all">All Categories</option>{(d.categories||[]).map(r=><option key={r.category}>{r.category}</option>)}</Form.Select><Form.Select aria-label="Status" value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="all">All Statuses</option>{[...new Set((d.items||[]).map(r=>r.status))].map(v=><option key={v}>{v}</option>)}</Form.Select></div></header>{items.length?<><div className="fa-table"><table><thead><tr><th>Reference</th><th>Name</th><th>Category</th><th>Start Date</th><th>Cost</th><th>Scheduled Depreciation</th><th>Estimated NBV</th><th>Monthly</th><th>Status</th></tr></thead><tbody>{items.map(r=><tr key={r._id||r.assetReference}><td><b>{r.assetReference}</b></td><td>{r.name}</td><td>{r.category}</td><td>{date(r.startDate)}</td><td>{money(r.purchaseCost,r.currency)}</td><td>{money(r.values?.scheduledAccumulatedDepreciation,r.currency)}</td><td>{money(r.values?.netBookValue,r.currency)}</td><td>{money(r.values?.monthlyDepreciation,r.currency)}</td><td><span className="fa-status">{r.values?.fullyDepreciated?'Fully Depreciated':label(r.status)}</span></td></tr>)}</tbody></table></div><div className="fa-mobile">{items.map(r=><article key={r._id||r.assetReference}><header><b>{r.assetReference}</b><span className="fa-status">{r.values?.fullyDepreciated?'Fully Depreciated':label(r.status)}</span></header><h3>{r.name}</h3><p>{r.category} · {date(r.startDate)}</p><dl><div><dt>Cost</dt><dd>{money(r.purchaseCost,r.currency)}</dd></div><div><dt>Scheduled depreciation</dt><dd>{money(r.values?.scheduledAccumulatedDepreciation,r.currency)}</dd></div><div><dt>Estimated NBV</dt><dd>{money(r.values?.netBookValue,r.currency)}</dd></div></dl></article>)}</div></>:<div className="fa-empty"><strong>No fixed assets recorded or matching these filters.</strong></div>}</section><section className="fa-bottom"><aside><BsInfoCircleFill/><div><h2>About Fixed Assets</h2><p>Fixed assets are long-term tangible assets used in business operations. Posted depreciation and General Ledger balances remain the financial reporting authority.</p></div></aside><aside><BsLayers/><div><h2>Key Insights</h2><p>{d.count?`${d.categories?.[0]?.category||'The largest category'} contains ${d.categories?.[0]?.count||0} recorded assets. ${d.items?.filter(r=>r.values?.fullyDepreciated).length||0} assets are fully depreciated by schedule.`:'More asset history is required to generate meaningful insights.'}</p></div></aside></section></>}</main>}
