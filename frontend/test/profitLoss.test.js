import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';import{change,comparisonRange,resolvePeriod,statementRows}from'../src/components/admin/profitLoss/profitLossView.js';const page=fs.readFileSync(new URL('../src/pages/admin/AdminProfitLossPage.jsx',import.meta.url),'utf8'),css=fs.readFileSync(new URL('../src/pages/admin/profitLoss.css',import.meta.url),'utf8'),routes=fs.readFileSync(new URL('../src/routes/AppRoutes.jsx',import.meta.url),'utf8');
test('P&L route is dedicated, permission protected and uses formal ledger API',()=>{assert.match(routes,/AdminProfitLossPage/);assert.match(routes,/GL_VIEW_PROFIT_LOSS/);assert.match(page,/fetchLedgerProfitLoss/);assert.doesNotMatch(page,/185,420|142,360|43,060/)});
test('period and comparison helpers avoid divide by zero',()=>{assert.deepEqual(resolvePeriod('THIS_MONTH',new Date('2026-09-13T00:00:00Z')),{from:'2026-09-01',to:'2026-09-30'});assert.deepEqual(comparisonRange({from:'2026-09-01',to:'2026-09-30'},'PREVIOUS_PERIOD'),{from:'2026-08-01',to:'2026-08-31'});assert.equal(change(10,0),null)});
test('statement preserves canonical accounting order and responsive cards',()=>{assert.deepEqual(statementRows({}).map(r=>r.key),['revenue','costOfSales','grossProfit','operatingExpenses','operatingProfit','otherIncome','otherExpenses','netProfit']);assert.match(css,/@media\(max-width:767px\)/);assert.match(css,/@media\(max-width:360px\)/)});
