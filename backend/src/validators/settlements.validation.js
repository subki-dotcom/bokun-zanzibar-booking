const { z } = require("zod");

const settlementId = z.object({ settlementId: z.string().min(1) });
const listSettlementsSchema = z.object({ params: z.object({}).optional(), body: z.object({}).optional(), query: z.object({ page: z.coerce.number().int().min(1).optional(), limit: z.coerce.number().int().min(1).max(100).optional(), provider: z.string().optional(), status: z.string().optional(), currency: z.string().length(3).optional(), search: z.string().max(150).optional() }).optional() });
const createSettlementSchema = z.object({ params: z.object({}).optional(), query: z.object({}).optional(), body: z.object({ settlementReference: z.string().min(1).max(150), provider: z.string().min(1), providerSettlementReference: z.string().max(200).optional(), idempotencyKey: z.string().max(200).optional(), grossAmount: z.coerce.number().nonnegative().optional(), grossCurrency: z.string().length(3).optional(), commissionAmount: z.coerce.number().nonnegative().optional(), commissionCurrency: z.string().length(3).optional(), feesAmount: z.coerce.number().nonnegative().optional(), feesCurrency: z.string().length(3).optional(), expectedNetAmount: z.coerce.number().nonnegative().optional(), expectedNetCurrency: z.string().length(3).optional(), receivedAmount: z.coerce.number().nonnegative().optional(), receivedCurrency: z.string().length(3).optional(), settlementDate: z.string().optional(), receivedVia: z.string().optional(), evidenceSource: z.string().min(1), evidenceReference: z.string().min(1).max(500), adjustments: z.array(z.object({ type: z.string(), amount: z.coerce.number().nonnegative(), currency: z.string().length(3), reason: z.string().min(1), source: z.string(), externalReference: z.string().optional() })).optional(), notes: z.string().max(3000).optional() }) });
const allocationSchema = z.object({ params: settlementId, query: z.object({}).optional(), body: z.object({ bookingReference: z.string().min(1), amount: z.coerce.number().positive(), currency: z.string().length(3), purpose: z.string().optional() }) });
const reconcileSchema = z.object({ params: settlementId, query: z.object({}).optional(), body: z.object({}).optional() });
const reverseAllocationSchema = z.object({ params: z.object({ allocationId: z.string().min(1) }), query: z.object({}).optional(), body: z.object({ reason: z.string().min(1).max(1500) }) });
const importPreviewSchema = z.object({ params: z.object({}).optional(), query: z.object({}).optional(), body: z.object({ csv: z.string().min(1).max(5_000_000) }) });
const importCommitSchema = z.object({ params: z.object({}).optional(), query: z.object({}).optional(), body: z.object({ token: z.string().min(32).max(200) }) });
module.exports = { listSettlementsSchema, createSettlementSchema, allocationSchema, reconcileSchema, reverseAllocationSchema, importPreviewSchema, importCommitSchema };